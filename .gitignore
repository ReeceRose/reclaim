bin/
.dev/
.env

# SQLite WAL files
*.db
*.db-wal
*.db-shm

# Frontend build output (embedded into binary at build time).
# Keep the export directory present so `go:embed all:out` works before a build.
web/out/*
!web/out/.gitkeep
web/.next/
web/node_modules/
web/*.tsbuildinfo

# OS
.DS_Store

bin/
.dev/
.env

# SQLite WAL files
*.db
*.db-wal
*.db-shm

# Frontend build output (embedded into binary at build time).
# The placeholder index.html is committed so `go build` works before a build.
web/out/*
!web/out/index.html
web/.next/
web/node_modules/

# OS
.DS_Store
